import React from 'react';

export default function SignupLayout({ children }: { children: React.ReactNode }) {
  return (
    <div>
      {/* You can add a header or styling here */}
      {children}
    </div>
  );
}